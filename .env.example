# Frontend API Configuration
# In production on Vercel, leave empty to use same-origin relative URLs (/api)
# Or specify full URL if hosting backend on another platform (e.g. Render/Railway)
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
